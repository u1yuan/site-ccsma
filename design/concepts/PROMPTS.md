# CCSMA concept-board prompts

Mode: built-in image generation. Taxonomy: `ui-mockup`. These boards are internal references only and are not included in `public/` or the static export.

## Programs

Create a tall, high-fidelity 1440 px desktop website board for CCSMA programs. Use a deep `night-950` environment with cyan, violet, and pale-blue biological light; a woodsprite canopy; six distinct flora sections labeled exactly `BSCS`, `BSIT`, `BMMA`, `BSCY`, `BDMM`, and `BSFTE`; and a thin branching data vine at the viewport edge. Align organic forms to a precise 12-column technical grid. Use Poppins-like display, Inter-like body, and JetBrains Mono-like utility typography. Keep campus green and muted gold to micro-accents. Navigation must contain exactly `Programs`, `Organizations`, and `Student Activities`. Replace long copy with abstract structured text lines and use only the generic link `Read official description`. Include a visible `Unofficial concept` state. No invented program facts, source brands, logos, movie characters or branding, Papyrus, watermarks, glassmorphism overload, generic dashboard cards, or cramped content.

## Organizations

Create a tall, high-fidelity 1440 px desktop website board for the CCSMA organizations route. Use four organic groves connected to one central Tree of Voices on a precise technical grid. Label the groves exactly `ACM`, `AITS`, `JPCS`, and `PRISM`; label the connector exactly `SCC`; and give every organization the exact visible state `Content coming soon`. Navigation must contain exactly `Programs`, `Organizations`, and `Student Activities`. Include the edge data vine, source attribution, visible focus states, and `Unofficial concept`. Render other copy as abstract text lines. No organization logos, officers, slogans, descriptions, calls to join, institutional claims, movie branding, Papyrus, or watermarks.

## Student Activities

Create a tall, high-fidelity 1440 px desktop website board for the Student Activities and Development Unit route. Build a central abstract Tree of Souls from branching data lines and flora on a precise grid. Use the exact section labels `Vision`, `Mission`, `Roles and Function`, `Student Development programs`, and `Student Activities responsibilities`; the roots `Serve`, `Lead`, and `Excel`; and the exact contact block from `.cursor/CONTENT.md`. Navigation must contain exactly `Programs`, `Organizations`, and `Student Activities`. Include the branching edge vine, source attribution, email focus state, and `Unofficial concept`. Render other prose as abstract text blocks. No logos, endorsement implication, movie characters or branding, Papyrus, watermarks, generic cards, or illegible type.
